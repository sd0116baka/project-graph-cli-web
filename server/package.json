{
  "name": "@graphif/project-graph-web-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm -w nx run @graphif/project-graph-core:build && node src/server.js",
    "start": "pnpm -w nx run @graphif/project-graph-core:build && node src/server.js",
    "test": "pnpm -w nx run @graphif/project-graph-core:build && pnpm -w exec vitest run --config server/vitest.config.mjs"
  },
  "dependencies": {
    "@graphif/prg-codec": "workspace:*",
    "@graphif/project-graph-core": "workspace:*"
  },
  "engines": {
    "node": ">=26.0.0"
  }
}
